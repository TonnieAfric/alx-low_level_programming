#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return Always 0 (Success)
 */
int main(void)
{
	@@ -16,7 +16,7 @@ int main(void)

	if (n == 0)
		printf("%d is zero\n", n);
	else if ( n < 0)
		printf("%d is negative\n", n);
	else
		printf("%d is positive\n", n);
	return (0);
}
